<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatedTo="navigatedTo">
  <Page.actionBar>
    <ActionBar title="Plugin Demos" icon=""/>
  </Page.actionBar>
  <StackLayout class="p-20">
    <ScrollView class="h-full">
      <StackLayout>
        <Button text="apple-pay" tap="{{ viewDemo }}" class="btn btn-primary rounded view-demo"/>
        <Button text="google-pay" tap="{{ viewDemo }}" class="btn btn-primary rounded view-demo"/>
        <Button text="payments" tap="{{ viewDemo }}" class="btn btn-primary rounded view-demo"/>
      </StackLayout>
    </ScrollView>
  </StackLayout>
</Page>